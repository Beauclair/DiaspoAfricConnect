rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can view uploaded images; only authenticated users can upload
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
